# -*- coding: utf-8 -*-
"""
config.py — 미래파 논쟁 분석 설정/사전  (gigyo(Critical-Debates) 구조 준용)

다른 논쟁에 적용하려면 원칙적으로 이 파일만 수정한다.
로직은 corpus.py / analyses.py 에 있으며 이 파일에 단방향 의존한다.
"""
from __future__ import annotations
from typing import Dict, List

# ── 토크나이저(Kiwi) 내용어 태그 ───────────────────────────
CONTENT_TAGS = ("NNG", "NNP", "NNB", "NR", "NP", "VV", "VA", "VX",
                "MM", "MAG", "MAJ", "SL", "SH")

# ── 분석 대상어 ────────────────────────────────────────────
TARGET = "미래파"                 # 담화 운율·분산의 중심 기표
NAMING_TERMS: List[str] = [        # 경쟁 명명어(movement) — Q1
    "미래파", "다른 서정", "뉴웨이브", "신서정", "아방가르드", "모더니즘",
]
KEY_TERMS: List[str] = [           # 개념어(사전; LL은 이와 독립적으로 귀납 추출)
    "환상", "실재", "서정", "주체", "정치", "타자", "윤리", "감각",
    "부정성", "전위", "숭고", "미학", "현대", "전복", "사건", "리듬",
]
TRAJECTORY_TERMS: List[str] = ["환상", "실재", "타자", "주체", "윤리", "정치", "감각", "서정"]
COLLOCATION_TARGETS: List[str] = ["미래파", "환상", "실재", "서정"]

# ── 담화 운율 평가어 사전 (잠정·검증 대상) ─────────────────
#   ⚠ 손으로 만든 목록 → 2인 검증 전까지 '잠정 지표'. 양가어(전복/유희)는 KWIC로 방향 확정.
POS_EVAL: List[str] = ["새로", "대안", "가능성", "신선", "참신", "활력", "혁신", "갱신", "주목"]
NEG_EVAL: List[str] = ["난해", "폐쇄", "자폐", "소모", "모호", "허상", "외설", "장광설", "혼란", "과잉", "실패"]

STOP_WORDS: List[str] = [
    "것", "수", "줄", "바", "데", "그것", "이것", "저것", "이", "그", "저",
    "때", "곳", "점", "면", "말", "일", "사실", "정도", "경우", "등", "및",
    "하", "되", "있", "없", "같", "보", "주", "받", "더", "또", "다", "잘",
    "년대",  # Kiwi 토큰화 잔여물(2000년대·1990년대 분리) — OCR 아님
]

# ── 분석 범위: 2005–2011 (논쟁의 완결된 호; 사후 역사화 2013- 제외) ──
YEAR_MAX = 2011
# ── 시기 구분 (내용검증 기반 3단계) ────────────────────────
def period(year: int) -> str:
    if year <= 2007: return "I 발생·논쟁(05-07)"
    if year <= 2009: return "II 분기·이론화(08-09)"
    return "III 정리·일단락(10-11)"
PERIOD_ORDER = ["I 발생·논쟁(05-07)", "II 분기·이론화(08-09)",
                "III 정리·일단락(10-11)"]

# ── 파일명 연도 보정(잘린 파일명) ──────────────────────────
YEAR_FIX: Dict[str, int] = {
    "박상수_무한": 2010, "이병철_감각적 허상": 2014, "조재룡_주체에서 주체로": 2013,
    "이수명_미래파를 위하여": 2009, "이찬_'미적 현대'": 2009,
    "이경수_전 지구적": 2007, "함돈균_이것은 시뮬라크르": 2006,
}
# ── 파일명 연도가 실제 발표연도와 다를 때 강제 보정(연도 있어도 우선) ──
YEAR_OVERRIDE: Dict[str, int] = {
    "고봉준_감각의 난장": 2009,  # 서정시학 통권44호, 2009년 12월(겨울호) → 파일명 _2010 보정
}

# ── 진영(camp) 배정: ⚠ 해석적 판단 → 연구자가 채워야 함 ────
#   비워두면 진영 분석은 skip. key = 파일명 접두, value = 옹호|비판|회고
CAMP_MAP: Dict[str, str] = {
    # ── 옹호 (11편) ──
    "권혁웅": "옹호", "이장욱": "옹호", "김수이": "옹호", "신형철": "옹호",
    "이찬": "옹호", "조연정": "옹호", "이형권": "옹호",
    "함돈균_이것은 시뮬라크르": "옹호",   # '06(다른 서정) — '10(한 행방)은 유보로 제외
    # ── 비판 (20편) ──
    "유성호": "비판", "고봉준": "비판", "이경수": "비판", "한태호": "비판",
    "하상일": "비판", "박대현": "비판", "박판식": "비판", "장석주": "비판",
    "장이지": "비판", "이수명": "비판", "손남훈": "비판", "오윤정": "비판", "김남석": "비판",
    # 나머지(유보 29편·좌담 포함)는 미배정 → 진영 대조에서 제외
}

# ── 계량 파라미터 ──────────────────────────────────────────
COLLOCATION_WINDOW = 5
MIN_FREQ_KEYWORD = 5        # 소표본이라 절대값보다 순위 중시
MIN_FREQ_COLLOCATE = 3
TOP_N_KEYWORDS = 25
LOWFREQ_FLAG = 5           # 이하 빈도는 결과에 ⚠ 표시
MIN_CELL_TOKENS = 3000     # 시기 셀 신뢰성 임계(내용어 토큰)
